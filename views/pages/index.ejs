<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Pensum Interactivo</title>
    <link rel="stylesheet" href="/css/index.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Barra de navegación -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
        <div class="container-fluid">
            <a class="navbar-brand" href="/pensum">Pensum USAC</a>
            <div class="navbar-nav">
                <button class="btn btn-outline-light me-2" onclick="location.reload()">Home</button>
                <button class="btn btn-outline-light me-2" id="errorReport">Error Report</button>
                <!-- Botones de archivo -->
                <button id="clear" class="btn btn-outline-light me-2">Limpiar Editor</button>
                <input type="file" id="fileInput" style="display:none">
                <button id="open" class="btn btn-outline-light me-2">Cargar Archivo</button>
                <button id="save" class="btn btn-outline-light">Guardar Archivo</button>
            </div>
        </div>
    </nav>

    <div class="container_user">
        <div class="row">
            <!-- Editor de texto -->
            <div class="col-md-6">
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        Editor de Texto
                    </div>
                    <div class="card-body">
                        <%- include ('../partials/editor') %>
                    </div>
                </div>
            </div>
            <!-- Tabla de tokens -->
            <div class="col-md-6">
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        Tabla de Tokens
                    </div>
                    <div class="card-body">
                        <%- include ('../partials/table') %>
                    </div>
                </div>
            </div>
        </div>
        <!-- Visualización del Pensum -->
        <div class="row">
            <div class="col-12">
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        Visualización del Pensum
                    </div>
                    <div class="card-body">
                        <div id="pensums"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/index.js"></script>
    <script src="/js/ui.js"></script>
</body>
</html>